{{!-- {{#each post.comments as |comment|}}
    <div class="class-name">
        <p class="class-name">Commented by: {{comment.user.username}} on {{formatDate comment.createdAt 'MM/DD/YYYY'}}</p>
        <p class="class-name">{{comment.comment_text}}</p>
    </div>
{{/each}} --}}
